package com.jeiel85.daddymode.ui.theme

import androidx.compose.ui.graphics.Color

// Light mode color palette
val LightPrimary = Color(0xFF1A365D)     // Deep corporate navy (represents work/calm authority)
val LightSecondary = Color(0xFFDD6B20)   // Warm home orange (represents sunset, warmth, family hearth)
val LightTertiary = Color(0xFF3182CE)    // Relaxing slate blue
val LightBackground = Color(0xFFF7FAFC)  // Warm screen off-white (comfortable on 40-50yo eyes)
val LightSurface = Color(0xFFFFFFFF)     // Clean white cards
val LightOnBackground = Color(0xFF2D3748) // High contrast dark charcoal text (excellent readability)
val LightOnSurface = Color(0xFF1A202C)    // Dark charcoal for card text

// Dark mode color palette
val DarkPrimary = Color(0xFF90CDF4)      // Soft slate blue
val DarkSecondary = Color(0xFFFBD38D)    // Soft warm cream orange
val DarkTertiary = Color(0xFF63B3ED)     // Light sky blue
val DarkBackground = Color(0xFF1A202C)   // Evening midnight slate (eye relaxation)
val DarkSurface = Color(0xFF2D3748)      // Warm dark gray card surfaces
val DarkOnBackground = Color(0xFFEDF2F7) // Pure off-white high contrast text
val DarkOnSurface = Color(0xFFF7FAFC)    // Clean white text
